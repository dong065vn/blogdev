# Server Configuration
PORT=3000
NODE_ENV=development

# Security - REQUIRED in production
# Generate a strong random token: openssl rand -base64 32
ADMIN_TOKEN=changeme-please-use-strong-random-token

# CORS Configuration
# Comma-separated list of allowed origins
# DO NOT use '*' in production!
ALLOWED_ORIGIN=http://localhost:5500,http://127.0.0.1:5500,https://dong065vn.github.io

# Optional: Custom temp directory
# RUNTIME_TMPDIR=/tmp
